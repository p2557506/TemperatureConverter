const kelvin = 293;//Constant temperature 
const celsius = kelvin - 273;//Constant temperature

let fahrenheit = Math.floor(celsius * (9/5) + 32);//Conversion to Fahrenheit r

console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`)
